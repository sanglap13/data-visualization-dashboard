import React from 'react';
import { MainContainer, UserDataGrid } from '../../shared';

const UsersInfo = () => {
  return (
    <div>
      <UserDataGrid />
    </div>
  );
};

export default UsersInfo;
