import React from 'react';
import { BarChart } from '../../../shared';

const SdkIntBar = () => {
  return (
    <div style={{ height: 'inherit' }}>
      <BarChart />
    </div>
  );
};

export default SdkIntBar;
